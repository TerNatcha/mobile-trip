import 'package:flutter/material.dart';

class MyPersonInfoPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('My personal information will be shown here.'),
      ),
    );
  }
}
