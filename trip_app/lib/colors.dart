import 'package:flutter/material.dart';

class AppColors {
  static const Color primaryColor = Color(0xFFFFA500); // Orange color
  static const Color backgroundColor =
      Color(0xFF141414); // Dark background color
  static const Color textColor = Colors.white;
  static const Color hintColor = Colors.grey;
  static const Color buttonColor = Color(0xFFFFA500);
}
